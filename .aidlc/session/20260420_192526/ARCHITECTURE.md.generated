`ARCHITECTURE.md` has been written to the project root. It covers the five required sections — overview, key components (API layer, club config, pages, entry/leaderboard components, hooks, utils), data flow for all three user journeys (entry submission, leaderboard polling, entry lookup), state management strategy, and the full directory structure — plus a forward-looking section mapping current state to the planned platform evolution.